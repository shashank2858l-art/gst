# SCAN_REPORT.md Template

> Generated by the GSD Smart Scanner. DO NOT edit manually — regenerate with `gst/scan`.

```markdown
---
scanned: {YYYY-MM-DD HH:MM}
folder: {absolute/path/to/project}
type: frontend | backend | fullstack
---

# Scan Report: {Project Name}

## Summary

| Property | Value |
|----------|-------|
| **Type** | {frontend / backend / fullstack} |
| **Framework** | {React / Express / Next.js / etc.} |
| **Language** | {TypeScript / JavaScript / Python / etc.} |
| **Database** | {MongoDB / PostgreSQL / none detected} |
| **Auth** | {JWT / session / OAuth / none detected} |
| **Package Manager** | {npm / yarn / pnpm / pip / etc.} |

## Directory Structure

```
{project-root}/
├── {dir1}/          # {purpose}
├── {dir2}/          # {purpose}
│   ├── {subdir}/    # {purpose}
│   └── {subdir}/    # {purpose}
├── {config-file}    # {purpose}
└── {entry-point}    # {purpose}
```

## Tech Stack

### Dependencies (Production)
| Package | Version | Purpose |
|---------|---------|---------|
| {package} | {version} | {what it does} |

### Dev Dependencies (Key Only)
| Package | Version | Purpose |
|---------|---------|---------|
| {package} | {version} | {what it does} |

## Routes / Pages Found

### Frontend Routes
| Path | Component/Page | Method |
|------|---------------|--------|
| {/path} | {ComponentName} | {GET} |

### Backend Endpoints
| Method | Path | Handler | Auth Required |
|--------|------|---------|--------------|
| {GET} | {/api/path} | {handlerName} | {yes/no} |

## API Calls Detected (Frontend → Backend)

| Frontend Location | Method | URL | Backend Match |
|-------------------|--------|-----|---------------|
| {file:line} | {POST} | {/api/auth/login} | {✅ matched / ⚠️ missing} |

## Database Models

| Model/Table | Fields | File |
|-------------|--------|------|
| {User} | {id, email, password, ...} | {models/User.js} |

## Auth Pattern

| Component | Implementation |
|-----------|---------------|
| Strategy | {JWT / session / OAuth} |
| Storage | {httpOnly cookie / localStorage / etc.} |
| Middleware | {file location} |
| Protected Routes | {list} |

## Environment Config

| Variable | Found In | Purpose |
|----------|----------|---------|
| {DB_URL} | {.env.example} | {Database connection} |
| {API_URL} | {.env.example} | {Backend URL} |

## Integration Points

Key files for integration:
- **Entry point**: {path}
- **Router config**: {path}
- **API client**: {path}
- **Database config**: {path}
- **Auth middleware**: {path}

## Notes
- {Any warnings, anomalies, or observations}
```

## Scan Cost

Target: ≤10 file reads total.

| Step | Tool | Cost |
|------|------|------|
| Structure map | list_dir | FREE |
| Tech detection | grep | CHEAP |
| Route discovery | grep | CHEAP |
| API calls | grep | CHEAP |
| DB/Auth | grep | CHEAP |
| Targeted reads | view_file | EXPENSIVE (limit 10) |
