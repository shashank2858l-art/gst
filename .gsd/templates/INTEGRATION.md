# INTEGRATION.md Template

> Generated by the GSD Integration Workflow. Maps frontend API calls to backend endpoints.

```markdown
---
generated: {YYYY-MM-DD HH:MM}
frontend: {absolute/path/to/frontend}
backend: {absolute/path/to/backend}
match_rate: {X}%
---

# Integration Report

## Summary

| Metric | Count |
|--------|-------|
| Frontend API calls | {N} |
| Backend endpoints | {M} |
| ✅ Matched | {X} |
| ⚠️ Missing backend | {Y} |
| ℹ️ Unused backend | {Z} |
| **Match Rate** | {X/(X+Y)}% |

## Matched Pairs ✅

| # | Frontend Call | Backend Endpoint | Confidence |
|---|-------------|-----------------|------------|
| 1 | `POST /api/auth/login` | `router.post('/auth/login')` | EXACT |
| 2 | `GET /api/users` | `router.get('/users')` | EXACT |
| 3 | `POST /api/orders` | `router.post('/order')` | HIGH |

## Missing Backend Routes ⚠️

Frontend calls these but no backend handler exists:

| # | Frontend Call | File:Line | Suggested Action |
|---|-------------|-----------|-----------------|
| 1 | `GET /api/dashboard` | `src/pages/Dashboard.tsx:45` | Create endpoint |
| 2 | `POST /api/upload` | `src/components/Upload.tsx:23` | Create endpoint |

## Unused Backend Endpoints ℹ️

Backend has these but no frontend calls them:

| # | Backend Endpoint | File:Line | Likely Purpose |
|---|-----------------|-----------|---------------|
| 1 | `DELETE /api/admin/users/:id` | `routes/admin.js:67` | Admin only |
| 2 | `GET /api/health` | `routes/health.js:5` | Health check |

## Config Alignment

| Setting | Frontend | Backend | Status |
|---------|----------|---------|--------|
| API Base URL | `VITE_API_URL=http://localhost:3001` | `PORT=3001` | ✅ Aligned |
| CORS Origin | — | `cors({ origin: 'http://localhost:5173' })` | ✅ Aligned |
| Auth Token | `localStorage.getItem('token')` | `Bearer token in header` | ⚠️ Check format |

## Integration Plan

### Phase 1: Config Setup
- [ ] Align environment variables
- [ ] Configure CORS on backend
- [ ] Set API base URL on frontend

### Phase 2: Missing Endpoints
- [ ] Create `GET /api/dashboard` endpoint
- [ ] Create `POST /api/upload` endpoint

### Phase 3: Auth Integration
- [ ] Ensure token format matches
- [ ] Add auth middleware to protected routes
- [ ] Add token interceptor on frontend

### Phase 4: Verify
- [ ] Test each matched pair end-to-end
- [ ] Test error handling (401, 404, 500)
- [ ] Test CORS from frontend origin

## Data Flow

```
Frontend                     Backend
────────                     ───────
User Action
  → Component
    → API Client (fetch/axios)
      → HTTP Request ──────────→ Route Handler
                                   → Middleware (auth, validation)
                                     → Controller
                                       → Database
                                     ← Response
      ← HTTP Response ←──────────
    ← State Update
  ← UI Re-render
```
```

## Confidence Levels

| Level | Meaning | Action |
|-------|---------|--------|
| **EXACT** (99%) | URL + method match perfectly | Auto-connect |
| **HIGH** (90%) | URL matches, minor differences | Verify then connect |
| **MEDIUM** (75%) | Partial URL match | Review before connecting |
| **LOW** (50%) | Semantic match only | Manual review required |
